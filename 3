3.	Write a C program to check whether a given string belongs to the language defined by a Context Free Grammar (CFG)
    S → 0A1	A → 0A | 1A | ε
    #include <stdio.h>
#include <stdbool.h>
bool isBelongToCFG(char *input);
bool parseA(char *input);

int main() {
    char str1[] = "00101";
    char str2[] = "0101";
    if (isBelongToCFG(str1)) {
        printf("String '%s' belongs to the language.\n", str1);
    } else {
        printf("String '%s' does not belong to the language.\n", str1);
    }

    if (isBelongToCFG(str2)) {
        printf("String '%s' belongs to the language.\n", str2);
    } else {
        printf("String '%s' does not belong to the language.\n", str2);
    }

    return 0;
}
bool isBelongToCFG(char *input) {
    return parseA(input) && *input == '\0';
}
bool parseA(char *input) {
    if (*input == '0') {
        return parseA(input + 1);
    } else if (*input == '1') {
        return parseA(input + 1);
    } else {
        return true;
    }
}
